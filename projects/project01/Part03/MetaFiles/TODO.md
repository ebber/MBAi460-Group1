# Project 01 Part 03 — ActionQueue

Part 03-scoped TODOs. For project-wide concerns, see [`MBAi460-Group1/MetaFiles/TODO.md`](../../../../MetaFiles/TODO.md). For visualization-specific concerns, see [`visualizations/MetaFiles/TODO.md`](../../../../visualizations/MetaFiles/TODO.md).

**Decision record:** Part 03 design decisions (Q1–Q6 from the Express pivot, plus any future architectural calls) live in [`DesignDecisions.md`](DesignDecisions.md). New collaborators — read that doc first to understand the *why* behind Part 03's structure.

## Active

- [ ] **[Cleanup] Decide final disposition of legacy `server/api_*.js` files** — kept as a behavioral reference during Server Foundation (02) and API Routes (03) execution, per design-agent recommendation 2026-04-26. At end of Part 03, decide one of: (a) delete — behavior fully replicated in `routes/` + `services/` + `middleware/` and covered by tests; (b) archive — move to `MetaFiles/Reference/legacy/` for posterity; (c) keep in place — explicitly documented as inert reference. Record the decision in `MetaFiles/refactor-log.md`. (Express pivot, design-agent review 2026-04-26)
  - **Status note (2026-04-26 production polish):** the legacy `api_post_image.js` requires `uuid`, which has now been removed from `dependencies` until workstream 03 reinstalls it. The legacy file is therefore *require-broken* and remains as a **textual** reference only. Workstream 03 should re-add `uuid` (and pick `uuid@14` with jest `transformIgnorePatterns` OR `uuid@9.x` for CJS compat) when wiring the new service module — see `install-log.md` 2026-04-26 production-polish entry.

- [x] **[UI] Fill `ClaudeDesignDrop/notes/export-notes.md` with metadata for Andrew's Frontend MVP** — CLOSED 2026-04-27 via Outstanding Integrations sub-A Phase 1 (commit `e8e84a7`). Filled all template fields + added Andrew→TS components-mapping table + Integration Status section.

- [x] **[Coordination] Surface Express pivot + Q1–Q6 decisions to Andrew** — CLOSED 2026-04-27 via Outstanding Integrations sub-A Phase 7 (commit `5e6613e`). Second-touch journal entry posted at `MBAi460-Group1/MetaFiles/Journal/2026-04-27-mvp-closeout-andrew-handshake.md` with TL;DR + what-landed-vs-spec table + audit-organization narrative + how-to-engage instructions + specific items requesting Andrew's eyes.

- [x] **[UI/Coordination] Reconcile Andrew's design into the approach docs and other structures** — CLOSED 2026-04-27 via Outstanding Integrations sub-A Phases 2–6 (commits 5c6004e through de5022d). 147-row audit (`MetaFiles/archive/Andrew-MVP-Integration.md`) routes all 1609 lines into 5 buckets across 13 themes; 6 NEW Future-State workstream docs created + 2 EXTEND; 3 Accelerators subfolders populated (7 files); naming-conventions section added in `00-coordination-and-contracts.md`; visualization update remains as a separate Active TODO entry per Erik's earlier deferral routing.

- [x] **[Tooling] Make an Orientation Map for Part 03** — CLOSED 2026-04-27: created at `Part03/MetaFiles/OrientationMap.md`. Cross-workstream scope (Active / Pending / Closed / Live findings sections) rather than workstream-scoped; per-substep update cadence per `feedback_atomic_substep_updates.md`; lite Compass at end of in-conversation responses overlies the Map's Compass section as in-memory orientation echo. Erik will guide archival when session/workstream-context closes.

- [ ] **[Backend] Add `morgan` request logging middleware** — Express server has no request logging today; the only stderr signal is the startup banner + `console.error('UNHANDLED ERROR:', err)` from the Phase 7 error middleware. When debugging the SPA-fallback bug 2026-04-27, the missing log forced a manual `curl /login` to confirm the 404 — would have been instant with `morgan('dev')`. Recommended: `npm install morgan` + `app.use(morgan('dev'))` near the top of `server/app.js` (BEFORE `/api/*` so all requests get logged). Optional polish: route format like `dev` for terminal, `combined` for production deploy. (Surfaced 2026-04-27 during 01 Phase 7 demo.)

- [ ] **[Lab tooling] `utils/_run_sql.py` parser hardening** — the script splits SQL on naked semicolons before filtering `--` comments, so semicolons INSIDE comments break the split. Hit during Pre-Phase 1 schema migration 2026-04-27 when the migration file's `--` comments contained semicolons; rewrote the migration without comment-semicolons as a workaround. Future polish: skip semicolons inside `--` and `/* */` comments before splitting. Captured in `Part03/MetaFiles/refactor-log.md` 2026-04-27 Pre-Phase 1 entry.

- [ ] **[Lab tooling] `utils/validate-db` empty-assets assertion drift** — `_validate_db.py` asserts the `assets` table is empty, but live RDS has prior testing data (10+ rows). Causes 1 spurious FAIL in `validate-db` output that's unrelated to schema correctness. Fix options: (a) loosen the assertion to just check the table exists with the right shape; (b) reset the live DB to seed-only state; (c) delete the assertion entirely. Surfaced during Pre-Phase 1 migration verification 2026-04-27.

- [ ] **[Visualization] Update `Target-State-project01-part03-photoapp-architecture-v1.md`** — UI MVP shipped 2026-04-27 with Vite + React 18.3.x + TypeScript strict + Tailwind + custom Tailwind-styled primitives (shadcn descoped 2026-04-27 per R1 reviewer remediation) + Zustand + react-router-dom + lucide-react. The Target-State viz needs updating to reflect the actual implementation stack (currently agnosticized per Q6, but the agnosticization can be loosened now that the implementation is committed). Erik handles outside the implementation session per 2026-04-27 routing. (This entry satisfies Plan §8.3.1.)

- [ ] **[UI] Live metadata for AssetCard document branch** — `AssetCard.tsx` (Subagent A, Phase 5+6) accepts optional `size` and `uploaded` props for the document branch but Phase 7.2 (LibraryPage) doesn't pass them. The server's `Asset` shape (per `00-coordination-and-contracts.md`) doesn't include size or upload date today. Either: (a) extend the asset shape in the server to include size + uploaded_at (schema migration); (b) leave document cards with just filename + kind badge (current behavior). (Surfaced 2026-04-27 in Subagent A's report.)

- [ ] **[UI] Vitest + plugin-react oxc/esbuild noise** — `npm test` from `frontend/` emits "Both esbuild and oxc options were set. oxc options will be used and esbuild options will be ignored" on every run. Cosmetic; tests pass. Tracking: pin `@vitejs/plugin-react` to a version that doesn't trip Vitest 4's oxc detection, OR wait for upstream fix. (Surfaced 2026-04-27 during Phase 1.4 Vitest setup.)

- [ ] **[UI] Centralize `deriveKind()` into a shared util** — Subagent C's `UploadScreen` includes a client-side mirror of the server's `deriveKind` (so the queue display can show "done with N labels" for photos vs. "OCR coming soon" for documents BEFORE the server response lands). The server's `deriveKind` lives in `server/schemas.js`; the client mirror is duplicated. Centralize into `frontend/src/utils/deriveKind.ts` (or similar) and re-export from both server + frontend tests so they can't drift. (Surfaced 2026-04-27 in Subagent C's report.)

- [ ] **[UI] Forgot password modal** — Login screen "Forgot password?" link → modal explaining password reset is not available in v1 (authsvc has no reset flow) and to contact staff. Per Andrew's spec §9.3 (placeholder). Bounded UX item; small. Surfaced 2026-04-27 sub-A audit row 54.
- [ ] **[UI] Dark mode (default = system preference)** — Per Andrew's spec FR-SYS-4 (SHOULD): support `prefers-color-scheme: dark` via Tailwind `dark:` variant + the dark token swap defined in `tokens.css` (warm near-black `#1C1B18` rather than cold gray, per §11.2). Surfaced 2026-04-27 sub-A audit row 105.
- [ ] **[UI] /offline route + ErrorBoundary with retry** — Per Andrew's spec §9.15 (404/error/offline) + §11.7 (Error boundary surface): (a) detect `navigator.onLine === false` → render `/offline` with a banner; cached library still browsable, disable upload + send. (b) Top-level React ErrorBoundary catches uncaught errors → "Something broke on our side. We've been notified." + correlation ID + retry. Surfaced 2026-04-27 sub-A audit rows 79, 102.
- [ ] **[UI] Login screen subhead "Cloud PhotoApp · Spring 2026"** — Per Andrew's spec §11.1: add subhead text below the wordmark on Login (sans, 16px, muted). Trivial polish. Surfaced 2026-04-27 sub-A audit row 111.
- [ ] **[UI] Adopt explicit font families (Inter / Source Serif 4 / JetBrains Mono)** — Per Andrew's spec §11.3: add font imports for `Inter` (sans), `Source Serif 4` (display, free Tiempos alternative), `JetBrains Mono` (mono). Currently using system stacks. Optional polish — system stacks are functional but Andrew specs free-tier-defensible specific families. Could DESCOPE if system stacks deemed sufficient. Surfaced 2026-04-27 sub-A audit row 113.
- [ ] **[UI] Per-route max-widths (1280 Library/Admin / 720 Asset+Profile / 480 Auth)** — Per Andrew's spec §11.5 layout: enforce per-route content max-widths. Currently routes share a uniform max width. Small CSS work. Surfaced 2026-04-27 sub-A audit row 115.
- [ ] **[UI] Voice/tone systematic audit pass** — Per Andrew's spec §11.8: walk shipped MVP copy and align to voice rules (concise, specific, human on errors, no exclamation points except time-sensitive success toasts, no emoji in product copy except ⬆ drop-zone glyph). Could DESCOPE if not warranted for a class project. Surfaced 2026-04-27 sub-A audit row 119.
- [ ] **[UI] Line-art empty-state illustrations (accent at 20% opacity)** — Per Andrew's spec §11.9: replace text-only empty states (`EmptyLibrary.tsx`) with simple line-art illustrations in `--color-accent` at 20% opacity. No mascots, no people. Design polish. Surfaced 2026-04-27 sub-A audit row 120.
- [ ] **[UI] Page chrome polish: banner slot + auth-route wordmark-only layout** — Per Andrew's spec §12.1: (a) authenticated page chrome adds an optional page-level banner slot at the top of content area (for global notices); (b) unauthenticated routes (`/login`, `/register`, `/404`, `/offline`) render only the wordmark centered on a blank cream canvas — no LeftRail, no full TopBar. Currently auth pages share the chrome with authenticated pages. Surfaced 2026-04-27 sub-A audit row 121.

- [ ] **[Hygiene] Plan tracker placeholder backfill (sub-A + sub-B)** — Backfill 12 `(this commit)` placeholders across both sub-workstream plans' Master Trackers + Acceptance Evidence sections (sub-A: 7 placeholders → real hashes; sub-B: 5). Mirrors the deferred-backfill pattern from `e016e76` earlier in the session. Mechanical edit; ~25 min total. Plans work fine with placeholders today; hygiene-improvement only. Surfaced 2026-04-27 during session-end spin (`MBAi460-Group1/MetaFiles/Journal/2026-04-27-session-spin-and-reflections.md`).

- [ ] **[Hygiene] `Future-State-roadmap.md` doc-extension pass** — Doc has stale text from before sub-A added 6 NEW Future-State workstream docs: (a) "## What is split into Future-State approach docs" intro line says "Four focused approach docs" but lists 8 (now 14 total counting the 6 NEW); table needs extension. (b) "## Phase ordering (suggested, not committed)" ASCII diagram (lines 51-69) predates the 6 NEW workstreams; sub-E added a pointer to the new "Activation priority recommendation 2026-04-27" section but didn't rewrite the ASCII. Either replace the ASCII with a link to sub-E's section or extend it. ~20-30 min. Surfaced 2026-04-27 during session-end spin (sub-E explicitly flagged as out-of-scope).

## Backlog

- [ ] **[Cleanup] Prune unused `response_page_size` from `server/config.js`** — `response_page_size: 12` is a leftover from the Project 2 baseline; not referenced by any post-Phase-9 server code. Workstream 03 will revisit `config.js` when adding the service module — fold this prune into that pass (or address standalone if 03 stalls). (Production review 2026-04-26)

## Closed / Confirmed

- [x] **[Reference] Bucketkey shape confirmed** — CONFIRMED 2026-04-26: `<username>/<uuid>-<localname>` per Part 2 convention. The literal `-` between uuid and localname is the intended separator (not `_` or `/`). Documented in `00-coordination-and-contracts.md` (`POST /api/images` implementation note) and used by `uploadImage` in `server/services/photoapp.js` (`03-api-routes.md` Phase 5). Kept here for cold-read context.

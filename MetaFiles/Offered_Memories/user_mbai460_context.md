---
name: MBAi 460 class project context
description: Erik's role and goals on the MBAi 460 Group 1 class project — AWS-backed photo web app
type: user
originSessionId: 1e15db51-7353-4e14-aeea-749705958a24
---
Erik is working on an MBAi 460 (graduate-level) group class project: an AWS-backed photo web application. Repo: `MBAi460-Group1/`, sibling lab-orchestration repo `mbai460-client/`. AWS region us-east-2 (course requirement). Each lab/project part is graded separately and builds on a shared AWS environment (S3 + RDS + IAM users).

He runs a serious agent-collaboration discipline on this work: governing manifesto, Future-State design doc, an active TODO/ActionQueue, a Journal of session entries, an OrientationMap, "Frame" execution patterns, named sub-workstreams, etc. Treat the repo's process artifacts (MetaFiles/, journals, plans/) as authoritative — don't invent new processes when an existing one fits.

Erik's stated bias: **lean toward queueing** — when a small inline-execute candidate appears during reflective/cleanup arcs, default is to enqueue it with full design notes rather than knock it out. Inline is appropriate during execution arcs when scope-contained + low-risk.
